// src/components/Hero.js
// Este archivo servirá para construir la sección principal (Hero) de la web.
export const Hero = () => {
  return `
    <div id="hero-section">
      <p>Hero Component</p>
    </div>
  `;
};

export const initHeroAnimations = () => {
  // Aquí programaremos las animaciones de Motion más adelante
};

