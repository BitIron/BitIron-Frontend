version: '3.8'

services:
  frontend:
    build: .
    container_name: bitiron_frontend
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - VITE_API_URL=http://localhost:3000/api
